You are my DevOps agent.

Goal: set up a local Kubernetes cluster using k3d (k3s in Docker containers) on macOS.
Deliver:
1) brew install commands
2) k3d cluster create command with sensible defaults
3) kubectl/helm verification commands
4) a Makefile with targets: k8s-up, k8s-down, k8s-status, ingress-up
5) a README section explaining prerequisites and troubleshooting.
Keep commands copy/pastable.